package assign3

import org.junit.jupiter.api.Test
import java.time.LocalDate
import java.time.LocalTime
import kotlin.test.assertEquals
import kotlin.test.assertTrue

class MeetingSchedulerTest {

  @Test
  fun canary() {
    assertTrue(true)
  }
 
  //Feedback: Please do not modify the following two tests. Only uncomment the 
  //first one, make it pass. Then leave it uncommented, then uncommented the 
  //second and make sure that passes too.
 
  @Test
  fun testCallScheduleMeetingTraditional() {
  //  val result = schedule.meeting({})
  //    
  //  assertTrue(result.hashCode() > 0)
  }

  @Test
  fun testCallScheduleMeetingFluent() {
  //  val result = schedule meeting {}
  //    
  //  assertTrue(result.hashCode() > 0)
  }

}